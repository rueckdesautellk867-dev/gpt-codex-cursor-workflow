# T005 建立 CI / 验证层设计文档

- **编号**：T005
- **状态**：已完成
- **风险等级**：低
- **推荐执行者**：Cursor

## 任务标题

建立 CI / 验证层设计文档

## 背景

v0.2 已有可复用任务队列；后续任务需要统一的验证口径，避免「不知何时算测过、何时须人工复核」。本阶段只定设计，不接真实 CI。

## 目标

新增 `docs/verification.md`：验证类型、按风险的最低要求、AI 交付必填的验证字段；并在 backlog / README / pilot-log 做入口与记录。

## 影响范围

- 模块 / 目录：`docs/`、`tasks/`、根 `README.md`
- 预计改动文件：`docs/verification.md`、`tasks/T005-verification-layer.md`、`tasks/backlog.md`、`README.md`、`docs/pilot-log.md`；可选预留 T006
- 可能波及的调用方：无（纯文档）

## 不做什么

- **不接入真实 CI**（无 workflow / 流水线配置）
- 不改业务代码、不新增依赖
- 不修改 T004 等高风险任务状态
- 不执行 T006

## 验收标准

- [x] 存在 `docs/verification.md`，含验证类型与低/中/高最低要求
- [x] 写明 AI 输出须含：执行了哪些验证、结果、未覆盖风险、是否需人工确认
- [x] T005 已完成；T004 仍为 `待确认`
- [x] 本任务只建立验证设计文档，不接入真实 CI

## 测试命令

```text
无需运行测试，仅检查 Markdown 链接和格式
```

## 完成后需要说明的内容

- 变更总结：已建立验证层设计文档
- 实际改动：见交付说明
- 验证：文档检查
- 风险：无；真实 CI 留给 T006

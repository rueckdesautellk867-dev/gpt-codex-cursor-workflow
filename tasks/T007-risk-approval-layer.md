# T007 建立风险与人工审批层

- **编号**：T007
- **状态**：已完成
- **风险等级**：低
- **推荐执行者**：Cursor

## 任务标题

建立风险与人工审批层

## 背景

v0.1–v0.3 已能识别高风险并停在确认前；需要把审批字段、禁止动作与回滚要求写成固定流程，避免口头约定不一致。

## 目标

新增 `docs/risk-approval.md`，并在 backlog / README / pilot-log（及分流文档）挂上入口；本任务只建文档，不执行任何高风险操作。

## 影响范围

- 模块 / 目录：`docs/`、`tasks/`、根 `README.md`
- 预计改动文件：`docs/risk-approval.md`、`tasks/T007-risk-approval-layer.md`、`tasks/backlog.md`、`README.md`、`docs/pilot-log.md`；可选 `docs/ai-task-routing.md`
- 可能波及的调用方：无（纯文档）

## 不做什么

- 不执行任何高风险操作
- 不执行 T004，不修改 T004 状态
- 不改业务代码、不新增依赖、不接真实 CI

## 验收标准

- [x] 存在 `docs/risk-approval.md`（分级、高风险范围、禁止动作、审批字段、AI 要求）
- [x] T007 状态为已完成；T004 仍为 `待确认`
- [x] 本任务只建立审批流程文档，不执行任何高风险操作

## 测试命令

```text
无需运行测试，仅检查 Markdown 链接和格式
```

## 完成后需要说明的内容

- 变更总结：已建立风险与人工审批层文档
- 验证：文档检查
- 风险：无；高风险执行仍取决于人工审批（如 T004）

# T002 README 增加任务系统入口链接

- **编号**：T002
- **状态**：已完成
- **风险等级**：低
- **推荐执行者**：Cursor

## 任务标题

在 README 中增加 `tasks/` 入口链接

## 背景

项目入口需能发现任务系统，否则执行者只会读规则文档，找不到 backlog 与任务单。

## 目标

在 `README.md` 文档索引（及必要时使用建议）中增加指向 `tasks/README.md` 与 `tasks/backlog.md` 的链接；不改其它章节结构。

## 影响范围

- 模块 / 目录：项目根文档
- 预计改动文件：`README.md`（小幅）
- 可能波及的调用方：无

## 不做什么

- 不大改 README 结构
- 不改业务代码与其它文档正文

## 验收标准

- [x] 文档索引可进入 `tasks/README.md`
- [x] 可进入 `tasks/backlog.md`
- [x] 其它原有链接保持有效

## 测试命令

```text
无需运行测试，仅检查 Markdown 链接和格式
```

## 完成后需要说明的内容

- 变更总结：README 增加 tasks 入口
- 实际改动文件：`README.md`
- 风险：无

# T004 高风险门禁样例（用户表会员等级字段）

- **编号**：T004
- **状态**：待确认
- **风险等级**：高
- **推荐执行者**：人工确认后由 Codex 或人工执行

> **门禁**：未人工确认前，禁止改库、改码、生成迁移或进入「进行中」。  
> 参考示例叙述：[`docs/examples/high-risk-db-migration-task.md`](../docs/examples/high-risk-db-migration-task.md)

## 任务标题

修改用户表结构以支持新的会员等级字段

## 背景

用于在真实任务队列中验证：高风险事项会被标为 `待确认` 并停在人工确认前。本阶段只建任务单，不执行。

## 目标

在 backlog 中保留一条高风险任务，状态固定为 `待确认`，直到人工确认并另开/更新执行计划。

## 影响范围

- 模块 / 目录：数据库迁移、用户数据、后端接口、回滚方案
- 预计改动文件：（确认并授权后才允许）用户表 migration、用户模型 / DTO、相关接口、回滚脚本
- 可能波及的调用方：依赖用户表结构的读写与会员逻辑

## 不做什么

- **当前不执行数据库变更**
- **不修改业务代码**
- **不生成迁移文件**
- 不将本任务状态改为 `进行中` 或 `已完成`（除非人工确认后另有授权）

## 验收标准

- [x] 风险等级为「高」，状态为 `待确认`
- [ ] **必须经过人工确认后才能进入执行阶段**（尚未确认）
- [x] 确认前不得改库、改码、生成 migration
- [ ] 若将来执行：须有回滚方案与验证计划（确认后再补）

## 测试命令

```text
不适用；本任务为高风险门禁样例，当前不执行
```

## 完成后需要说明的内容

- 本阶段：仅登记任务单与 backlog 行，**未执行任何高风险操作**
- 残留风险：真实执行前仍须人工确认

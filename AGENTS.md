# AGENTS.md

Codex 仓库级协作规则。所有自动化改动必须遵守。

## 项目目标

建立可复用的 GPT + Codex + Cursor 三方自动化研发闭环：需求可拆解、改动可验证、风险可确认、结果可回流。

## AI 协作分工

| 角色 | 职责 |
|------|------|
| GPT | 需求拆解、任务边界、验收标准、风险点 |
| Codex | 仓库级自动实现、批量修改、跑测试、出变更总结 |
| Cursor | IDE 内局部修改、调试、重构、人工协作审查 |

## 修改前

1. 先读相关代码与调用链，明确影响范围。
2. 需求模糊时先澄清，不猜测实现。
3. 只改完成任务所需的文件，不修改无关文件。
4. 不引入未在任务中说明的新依赖；确需新增时先说明理由并等待确认。

## 修改中

1. 遵循现有代码风格、目录结构和命名约定。
2. 不做范围外重构或“顺手优化”。
3. 高风险变更（权限、支付、数据库结构、用户数据、鉴权、生产配置）必须先停下来人工确认。

## 完成前

1. 运行与本次改动相关的测试 / 校验命令。
2. 无法运行时，在总结中写明原因与替代验证方式。
3. 输出变更总结：改了什么、为什么、如何验证、残留风险。
4. 远程 push 须用户明确同意；失败时按 `docs/push-fallback.md` 降级（记 tip、标待 Desktop、不死等），勿假装已推送。

## 输出格式

完成任务时按以下结构回复：

```text
## 变更总结
- ...

## 影响范围
- ...

## 验证结果
- 命令：...
- 结果：通过 / 失败 / 未跑（原因）

## 风险与待确认
- ...
```

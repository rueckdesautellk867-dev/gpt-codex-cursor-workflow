# 示例任务：修改用户表结构以支持新的会员等级字段

> 高风险试跑样例。用于验证「高风险必须先人工确认」规则；**本文件仅作示例，不进入执行。**

## 任务标题

修改用户表结构以支持新的会员等级字段

## 背景

业务需要在用户表增加会员等级相关字段。按分流规则，涉及数据库迁移与用户数据，属于高风险变更，必须先停在人工确认，不得直接由 AI 执行。

## 目标

用一份标准任务单展示：高风险事项如何写清影响、边界与门禁，确保未确认前不进入 Codex / 人工执行阶段。

## 影响范围

- 模块 / 目录：数据库迁移、用户数据、后端接口、回滚方案
- 预计改动文件：（示例，未实际改动）用户表 migration、用户模型 / DTO、相关读写接口、回滚脚本或 down migration
- 可能波及的调用方：依赖用户表结构的查询、缓存、报表、权限或会员逻辑

## 不做什么

- 当前任务只创建示例文档，不执行数据库变更
- 不修改代码
- 不生成迁移文件
- 不改生产配置、不触碰真实用户数据

## 验收标准

- [ ] 任务单已标明风险等级为「高」
- [ ] **必须经过人工确认后才能进入执行阶段**
- [ ] 确认前任何执行者（GPT / Codex / Cursor）均不得改库、改码或生成 migration
- [ ] 若进入真实执行，须另开任务并附带回滚方案与验证计划

## 测试命令

```text
不适用，本任务仅为高风险任务示例文档
```

## 风险等级

高

（数据库迁移、用户数据、后端接口、回滚；未确认禁止执行）

## 推荐执行者

人工确认后由 Codex 或人工执行

（确认前：不执行；确认后：迁移与数据操作以人工主导为宜，Codex 仅可在明确授权范围内改仓库）

## 完成后需要说明的内容

- 仅新增示例文档，未执行任何高风险操作
- 实际改动文件：`docs/examples/high-risk-db-migration-task.md`
- 测试：不适用
- 残留风险：无（未触达库与代码）
- 人工复核：本示例本身不需要；真实同类任务必须先人工确认
